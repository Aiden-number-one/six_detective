{{#if flag}}
{{else}}
<option value="">--Please select a type--</option>
{{#each this}}
	{{#if jdbcString}}
		<option value="{{driverId}}" urlInfo="{{jdbcString}}" className="{{className}}" driverId="{{driverId}}" driverName="{{driverName}}">{{driverName}}</option>
	
	{{else}}
		<option value="{{driverId}}" urlInfo="{{urlInfo}}" className="{{className}}" driverId="{{driverId}}" driverName="{{driverName}}">{{driverName}}</option>
	 {{/if}}
{{/each}}
{{/if}}
