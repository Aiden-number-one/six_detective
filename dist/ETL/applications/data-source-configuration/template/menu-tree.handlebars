{{#if flag}}
    
{{else}}
<ul>
    {{#each this}}
    <li parentmenuid="{{parentmenuid}}" menuid='{{menuid}}' menuname='{{menuname}}' data-jstree='{ "icon" : "icon-layers"}'>{{schema}}
        <ul>
            {{#each childItem}}
            <li parentmenuid="{{parentmenuid}}" menuid='{{menuid}}' menuname='{{menuname}}' data-jstree='{ "icon" : "fa fa-folder-o"}'>{{schema}}
            </li>
            {{/each}}
        </ul>
    </li>
    {{/each}}
</ul>

{{/if}}