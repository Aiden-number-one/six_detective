{{#if flag}}
	
{{else}}
<ul>
	{{#each this}}
    <li id="{{id}}" data-jstree='{ "type" : "{{type}}"}'> {{classification_name}}
        <ul>
        	{{#each children}}
            <li id="{{id}}" data-jstree='{ "type" : "{{type}}"}'> {{classification_name}}
            </li>
            {{/each}}
        </ul>
    </li>
    {{/each}}
</ul>
{{/if}}